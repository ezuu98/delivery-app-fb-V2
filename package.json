{
  "name": "monorepo-mvc",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "npm run -w apps/api dev & npm run -w apps/web dev",
    "build": "npm run -ws build",
    "start": "npm run -ws start"
  }
}